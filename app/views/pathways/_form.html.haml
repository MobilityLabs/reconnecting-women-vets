= form_for(@pathway, remote: true) do |f|
  - if @pathway.errors.any?
    #error_explanation
      %h2= "#{pluralize(@pathway.errors.count, "error")} prohibited this pathway from being saved:"
      %ul
        - @pathway.errors.full_messages.each do |msg|
          %li= msg

  .field
    = f.label :name
    = f.text_field :name
  .actions
    = f.submit 'Save'
