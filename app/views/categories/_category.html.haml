%tr{id: "category_#{category.id}"}
  %td= category.name
  %td= link_to 'Show', category
  %td= link_to 'Edit', edit_category_path(category)
  %td= link_to 'Destroy', category, method: :delete, remote: true, data: { confirm: 'Are you sure?' }
