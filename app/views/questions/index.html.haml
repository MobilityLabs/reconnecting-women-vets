.row-fluid
  .span12
    %p#notice= notice
.row-fluid
  .span6
    %h2 Listing questions

    %table(border=true)
      %thead
        %tr
          %th Pathway
          %th Text
          %th Order
          %th Actions

      %tbody#questions
        - @questions.each do |question|
          = render partial: question

  .span6
    %h2 Add a question:
    = render partial: 'form', locals: {actions: false}